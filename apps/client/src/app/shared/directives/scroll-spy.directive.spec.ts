import { ScrollSpyDirective } from '@client/app/shared/directives';

describe('ScrollSpyDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollSpyDirective();
    expect(directive).toBeTruthy();
  });
});
